一 附加权限
Set UID（了解）
附加在属主的x位置上
属主的权限标识会变成s
适用于可执行的文件，Set UID可以让使用者具有文件属主的身份及部分权限

Stichy Bit
附加在其他人的x位上
其他人的权限标识会变成t
适用于开放w权限的目录，可以阻止用户滥用w写入权限（禁止用户操作别人的文档）

二 文本/文件查找
1.grep
1）格式：grep [选项] 查找条件 目标文件
-i:忽略大小写
-v：取反匹配
2）常用的匹配模式
word 包含字符串word
^word 以字符串word开头
word$ 以字符串word结尾
注意：这几个可以连用

2.find
格式：find [目录] [条件] [-a|-o] [条件2].....
常用的条件表示
-type 类型（f,d,l,b,c）
-name “文档名称”
-size +10M   #大于10M      k(小写),M（大写）,G（大写）
-size -10M   #小于10M
-user 用户名  #按照文档的所有者
-iname “文档名称” #忽略大小写
-group 组名   #按照文档的所属组
-maxdepth  层数  #限制目录查找的深度（最大层）
按文件修改时间
-mtime     +10     #过去的10 天之前修改和创建的文档
-mtime     -10     #过去的10 天之后修改和创建的文档
注意：find /etc/ -size -1M   #size不能与1连用，可用1M=1024k

-a ：and
-o ：or

/proc:所占用的空间不是硬盘，而是内存

使用find命令的-exec操作
find .. .. -exec 处理命令{}  \;
优势：以{}代表，每一个结果，逐个处理，遇到\;结束

3统计命令
wc -l 文件名   #统计行数


/var  ：存放经常变化的数据

which find    #查找命令具体位置
/bin/find

首先激活网卡，再去修改dns配置文件














